;( function( W ) {
  
  var VideoBox = new Class({
    
    Extends: ObjectWithHandlers,
    
    initialize: function() {
      this.parent();
    }
    
  });
  
  W.VideoBox = VideoBox;
} )( window );